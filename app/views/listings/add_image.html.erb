<div class="container">
  <div class="row justify-content-center my-5">
    <div class="col-12 col-sm-12 col-md-8 col-lg-8 col-xl-6">
        <h3 class="text-center">What would you like to teach?</h3>
        <br>
        <%= simple_form_for(@listing) do |f| %>
        <%= f.input :title %>
        <%= f.input :image, collection: ["Beginner", "Intermediate", "Advanced", "Master"] %>
        <%= f.submit "Submit", :class => "button standard accent" %>
        <% end %>
    </div>
  </div>
</div>

<%= p @urls %>


<!-- <div class="new-listing-image d-flex flex-wrap">
  <% @urls.each_with_index do |url, index | %>
    <%= image_tag url, width: 200 %>
  <% end %>
</div> -->
